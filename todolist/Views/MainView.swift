import SwiftUI

struct MainView: View {
    var body: some View {
        VStack {
         Text("TodoList App")
        }
        
    }
}

struct ContentView_Previews: PreviewProvider {
    static var previews: some View {
        MainView()
    }
}
