import SwiftUI

struct RegisterView: View {
    var body: some View {
        VStack {
            // Header
            HeaderView()
        }
    }
}

struct RegisterView_Previews: PreviewProvider {
    static var previews: some View {
        RegisterView()
    }
}
